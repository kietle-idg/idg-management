rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Any authenticated user can read and write.
      // No expiration date — this rule is permanent.
      allow read, write: if request.auth != null;
    }
  }
}
